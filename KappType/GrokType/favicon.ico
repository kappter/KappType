[Binary favicon.ico file]
/* Note: Since favicon.ico is a binary file, you should generate it at https://favicon.io/text-to-favicon/ with text "TT" or a keyboard icon. Download the file and place it in the project root. Alternatively, I can provide a base64-encoded version or generate one if you confirm. For now, assume you’ll create it. */